# 目录

- [评价方法](#评价方法)
    - [层次分析法](#层次分析法)
    - [模糊综合评价](#模糊综合评价)
    - [熵权法](#熵权法)
    - TOPSIS(未提及)
- [最优化方法](#最优化方法)
    - [线性规划、整数规划和非线性规划](#线性规划和整数规划和非线性规划)
    - [灰色关联分析法](#灰色关联分析法)
    - 模拟退火和遗传算法（未提及）
- [相关性分析方法](#相关性分析方法)
    - [卡方独立性检验](#卡方独立性检验)
    - [皮尔逊(Pearson)相关系数](#皮尔逊相关系数)
    - [斯皮尔曼(spearman)相关系数](#斯皮尔曼相关系数)
- [预测类分析方法]()
    - [线性回归模型]()
- [往年题分析](#往年题分析)
    - [2024-C](#2024-C)
- [数据统计](#数据统计)

# 评价方法

## 层次分析法

> 考虑指标，权重，方案是层次分析法的三个核心要素。

### 核心思想与适用场景

#### 核心思想：

1. 分解：将复杂问题层次化，建立一个层次结构模型。

1. 比较：通过两两比较的方式，确定各因素（准则、方案）之间的相对重要性。

1. 综合：计算各层次的元素对总目标的组合权重，从而为最终方案排序。

#### 适用场景：

1. 选择最佳方案：如供应商选择、投资项目评估、选址等。

1. 资源分配：如预算分配、人力资源配置等。

1. 预测与评估：如企业风险评估、产品性能评估等。

1. 系统分析：任何涉及多个因素、需要排出优先级的复杂决策问题。

### 基本步骤（共六步）

我们以一个简单的例子贯穿整个步骤：小明想买手机，在品牌A、品牌B、品牌C之间做选择，他主要考虑三个因素：价格、性能、外观。

#### 第一步：建立层次结构模型
将决策目标、考虑的因素和决策对象按它们之间的相互关系分为最高层、中间层和最低层。

最高层（目标层）：决策的最终目标。→ 选择最好的手机

中间层（准则层）：衡量是否达到目标的各项准则。→ 价格、性能、外观

最低层（方案层）：可供选择的具体方案。→ 品牌A、品牌B、品牌C

#### 第二步：构造判断（成对比较）矩阵

这是AHP中最关键的一步。针对上一层某个因素，将本层次的所有因素进行两两比较，用数字1-9及其倒数作为“标度”来衡量相对重要性。

萨蒂1-9标度法含义表：

|  标度   | 含义                                                                     |
| :-----: | :----------------------------------------------------------------------- |
|    1    | 表示两个因素相比，具有同等重要性                                         |
|    3    | 表示两个因素相比，一个因素比另一个稍微重要                               |
|    5    | 表示两个因素相比，一个因素比另一个明显重要                               |
|    7    | 表示两个因素相比，一个因素比另一个强烈重要                               |
|    9    | 表示两个因素相比，一个因素比另一个极端重要                               |
| 2,4,6,8 | 上述相邻判断的中间值                                                     |
|  倒数   | 若因素i与j的重要性之比为$a_{ij}$，则j与i之比为$a_{ji}=\frac{1}{a_{ij} }$ |

例如：对于“选择手机”这个目标，小明对三个准则进行两两比较：

他认为 性能 比 价格 稍微重要（标度3）。

性能 比 外观 明显重要（标度5）。

价格 比 外观 稍微重要（标度3）。

据此，我们可以构建一个判断矩阵：

|      | 价格 | 性能 | 外观 |
| ---- | ---- | ---- | ---- |
| 价格 | 1    | 1/3  | 3    |
| 性能 | 3    | 1    | 5    |
| 外观 | 1/3  | 1/5  | 1    |

*解释：第一行第二列的值为1/3，表示“价格”与“性能”相比，重要性是后者的1/3（即性能比价格重要3倍）。*

同样地，我们需要为每一个准则（价格、性能、外观），对三个品牌（方案层）再分别构建一个判断矩阵。

#### 第三步：层次单排序及一致性检验

这一步的目的是计算每个判断矩阵的权重向量，即计算对于上层某个因素，本层次各因素的重要性权重。

计算权重向量：常用方法是“特征向量法”或“算术平均法”、“几何平均法”等近似方法。

以上面的矩阵为例，通过计算（具体计算过程略，可用软件完成），我们可以得到权重向量：

价格权重 ≈ 0.258

性能权重 ≈ 0.637

外观权重 ≈ 0.105

可以看出，对小明了来说，性能最重要，其次是价格，最后是外观。

一致性检验：由于判断矩阵是人为构造的，可能会出现逻辑错误（例如：A比B重要，B比C重要，但C又比A重要）。必须进行一致性检验，确保判断基本可靠。

计算一致性指标CI和一致性比率CR。

当 CR < 0.1 时，认为判断矩阵的一致性是可以接受的。如果未能通过，则需要重新调整判断矩阵。

#### 第四步：层次总排序及一致性检验

目的是计算最低层（方案层）各方案对最高层（目标层）的总权重，从而对方案进行优先排序。

我们已经得到了准则层对目标层的权重（W准则 = [0.258, 0.637, 0.105]）。

我们再通过同样的方法（步骤二和步骤三），计算出每一个方案（品牌A/B/C）相对于每一个准则（价格/性能/外观）的权重。

假设对于“价格”准则，三个品牌的权重是：W价格 = [0.3, 0.5, 0.2]（品牌B价格最优）

对于“性能”准则，权重是：W性能 = [0.5, 0.3, 0.2]（品牌A性能最优）

对于“外观”准则，权重是：W外观 = [0.2, 0.3, 0.5]（品牌C外观最优）

总排序权重 = (方案对价格的权重 * 价格的权重) + (方案对性能的权重 * 性能的权重) + (方案对外观的权重 * 外观的权重)

品牌A总得分 = (0.3 * 0.258) + (0.5 * 0.637) + (0.2 * 0.105) = 0.077 + 0.318 + 0.021 = 0.416

品牌B总得分 = (0.5 * 0.258) + (0.3 * 0.637) + (0.3 * 0.105) = 0.129 + 0.191 + 0.031 = 0.351

品牌C总得分 = (0.2 * 0.258) + (0.2 * 0.637) + (0.5 * 0.105) = 0.052 + 0.127 + 0.053 = 0.232

同样，也需要对总排序结果进行一致性检验（通常可以省略，只要单排序通过即可）。

#### 第五步：得出结论

根据总排序权重，品牌A（0.416）> 品牌B（0.351）> 品牌C（0.232）。因此，对小明来说，综合考量后品牌A是最优选择。

### 优点与局限性

#### 优点：

1. 系统性：将问题分解为清晰的层次，思维过程系统化、条理化。
1. 简洁实用：所需定量数据不多，但能明确表达决策者的主观判断。
1. 定性定量结合：将决策者的经验判断给予量化，非常适合处理难以完全定量分析的复杂问题。

#### 局限性：

1. 主观性较强：判断矩阵的构建严重依赖决策者的主观判断，不同的人可能得出不同结论。
1. 指标过多时工作量大：因素过多时，需要构建的判断矩阵数量多，两两比较非常繁琐（但可用软件解决）。
1. 只能从原有方案中选择：无法生成新方案。


## 模糊综合评价

### 核心思想：用数学处理“模糊”概念

我们日常生活中充满了“模糊”的概念，比如：

服务质量：“很好”、“一般”、“较差”

天气舒适度：“很热”、“热”、“舒适”、“凉”、“冷”

产品满意度：“非常满意”、“比较满意”、“一般”、“不满意”

这些概念无法用传统的“是/非”或“0/1”二值逻辑来精确描述。模糊综合评价的核心就是利用模糊数学的理论，将这些模糊的、主观的评价转化为客观的、可量化的评价结果，从而做出一个全面的、综合的判断。


### 适用场景

假设我们要评价一家餐厅“是否值得推荐”，传统方法可能是计算几个指标（口味、环境、服务）的平均分。但这种方法有缺陷：

1. 忽略指标重要性差异：对有些人来说口味比环境重要得多，平均分无法体现这种权重差异。
1. 无法处理模糊评价：一个8分的环境到底是“很好”还是“好”？界限是模糊的。
1. 信息损失：简单的平均分丢失了评价的分布信息（例如，10个人里有1个打0分，9个打10分，平均也是9分，但这和所有人都打9分含义完全不同）。

模糊综合评价正是为了解决这些问题而设计的。

### 步骤

1. 模糊综合评价指标的构建

    模糊综合评价指标体系是进行综合评价的基础，评价指标的选取是否适宜，将直接影响综合评价的准确性。

    这个过程的最主要工作是要确定评价因素集（U）和评语集（V）

    - 因素集（U）：即评价的指标体系。你要从哪些方面进行评价？

        > 例如，评价一款手机：U = {性能, 外观, 价格, 续航}

    - 评语集（V）：即可能做出的评价结论的集合。评价等级是什么？

        > 例如：V = {很好, 好, 一般, 差}

2. 确定各因素的权重

    不同指标的重要性不同。我们需要给每个指标分配一个权重，权重之和一般为1。

         例如，对于手机，你更看重性能和续航：A = {性能: 0.4, 外观: 0.1, 价格: 0.2, 续航: 0.3}

    权重的确定非常关键。如果题目中给出，则直接使用，否则可以使用层次分析法得出。

3. 构建隶属矩阵（核心）

    一般选取F分布确定隶属函数。

    - 三角形隶属函数

        最简单常用，由三个参数决定（起点、顶点、终点）。

        形状：/ \

        例如：定义“温度 around 25°C”，参数可以是 (20, 25, 30)。

    - 梯形隶属函数

        比三角形多一个平台区，由四个参数决定（左下角、左上角、右上角、右下角）。

        形状：/‾\

        例如：定义“舒适的温度”，参数可以是 (20, 22, 26, 28)，表示22°C到26°C之间都是完全舒适的。

    - 高斯形隶属函数

        形状呈钟形，光滑对称，由两个参数决定（均值/中心点，标准差/宽度）。

        形状：⦦

        例如：定义“平均身高”，参数是 (175cm, 5cm)。

    建立适合的隶属函数从而构建好隶属矩阵。

4. 构建模糊关系矩阵

    可以通过问卷调查等方式收集评价信息。

    如对某手机的调查结果如下：

    |      | 很好 | 好  | 一般 | 差  |
    | ---- | ---- | --- | ---- | --- |
    | 性能 | 0.6  | 0.3 | 0.1  | 0.0 |
    | 外观 | 0.1  | 0.2 | 0.5  | 0.2 |
    | 价格 | 0.0  | 0.1 | 0.3  | 0.6 |
    | 续航 | 0.7  | 0.2 | 0.1  | 0.0 |

    则模糊关系矩阵为$$
    R = 
    \begin{pmatrix}
    0.6 & 0.3 & 0.1 & 0.0 \\
    0.1 & 0.2 & 0.5 & 0.2 \\
    0.0 & 0.1 & 0.3 & 0.6 \\
    0.7 & 0.2 & 0.1 & 0.0 \\
    \end{pmatrix}
    $$

5. 由隶属度矩阵得出结论

    综合评价结果矩阵 = 权重矩阵$w$ $\times$ 模糊评价矩阵$R$

    根据最大隶属度原则得出结论即可。

## 熵权法

### 核心思想及适用场景

“熵”的概念源于热力学，后来被香农引入信息论，用来度量系统的混乱程度或不确定性。

在评价体系中，我们可以把一个指标在不同评价对象上的数据差异看作是其提供信息量的多少。

例如：推断对方的性别。
（1）已知：有个人在看手机。
（2）已知：有个人吸烟。

在这个例子中，“看手机”这一指标在男女之间差异不大，而“吸烟”这一指标很大，因此在数据分析时我们可以认为“吸烟”这一指标的权重更高。

具体地说，如果一个指标下，所有评价对象的数据完全一样（毫无差异），那么这个指标在区分各对象优劣时几乎提供不了任何有用信息，我们认为它的熵值很大，重要性很低，应该赋予较小的权重。反之，如果一个指标下，各评价对象的数据差异很大（非常离散），说明这个指标在有效地区分好坏，它包含的信息量很丰富，熵值很小，因此应该赋予较大的权重。

熵权法适用于多指标、多方案的决策与评价问题，优点是能够根据实际数据

**指标的离散程度（变异程度）越大 → 信息熵越小 → 信息量越大 → 权重越高。**

### 步骤

#### 第一步：数据标准化（归一化处理）

由于各指标的量纲和数量级不同，我们需要建立一个统一的标准，因此考虑将原始数据映射到$[0,1]$区间。

1. **效益型指标**（越大越好）
    $$p_{ij} = \frac{x_{ij} - \min(x_j)}{\max(x_j) - \min(x_j)}$$

1. **成本型指标**（越小越好）
    $$p_{ij} = \frac{\max(x_j) - x_{ij}}{\max(x_j) - \min(x_j)}$$

1. **中间型指标**（靠中间越好）
    假设当前指标为中间型指标，其最佳值为$M_j$（如人体体温37°C）。

    计算每个数据与最佳值的绝对差：
    $$d_{ij} = |x_{ij} - M_j|$$

    对这些$d_{ij}$进行标准化：
    $$p_{ij} = \frac{\max(d_j) - d_{ij}}{\max(d_j) - \min(d_j)}$$

    将上述两个公式合并可得：
    $$p_{ij} = 1 - \frac{|x_{ij} - M_j|}{\max(|x_j - M_j|)}$$

*以上下标$i,j$的含义为第$j$个指标的第$i$个数据*

异或选择统一的方式：
    $$z_{ij} = \frac{x_{ij}}{\sqrt{\sum^n_{i = 1}x_{ij}^2}}$$

#### 第二步：计算比重矩阵

计算每个数据在其指标中所占的比重，可以看作是概率的一种估计。
    $$p_{ij} = \frac{z_{ij}}{\sum^n_{i = 1}z_{ij}}$$

#### 第三步：计算第$j$个指标的熵值

利用信息熵公式计算每个指标的熵值。
    $$e_j = -\frac{1}{\ln n}\sum^{m}_{i = 1}r_{ij}\cdot \ln r_{ij}$$

#### 第四步：计算第$j$个指标的差异系数

熵值$e_j$越大，说明该指标数据的离散程度越小，差异性越小。

定义差异系数$g_j$为$$g_j = 1 - e_j$$

$g_j$越大，则信息量越大，越重要，权值应当越高。

#### 第五步：确定权值$w_j$

将差异系数归一化，得到每个指标的熵权。
$$w_j = \frac{g_j}{\sum^{n}_{j = 1}g_j}$$

#### 注意事项

熵权法本质上是一种客观的，从数据出发的指标权值计算方法。与最终决策之间有一定差距。

# 最优化方法

## 线性规划和整数规划和非线性规划

都是针对多因素的最优化问题求解。核心要素都是变化因子，限制条件，目标函数。

整数规划主要的两类是决策问题（01背包）和分配问题（匈牙利算法）。

## 灰色关联分析法

### 灰色系统

在系统科学中，白色系统表示信息完全可知，黑色表示完全未知，灰色则是部分可知。

### 适用情境

小样本、贫数据：我们明确拥有当前系统的一些数据，但是这些数据并不全面。

因素关系复杂：因素之间的相关关系不明确且较为复杂。

需要快速筛选主要因素：可以以时间等因素为评价指标，多种因素为比较序列，总体分数为参考序列，筛选出关联度最高的几个因素进行研究。

这种情况下，我们可以选择灰色关联分析法。

### 基本步骤

#### 第1步：确定比较序列和参考序列

比较序列为原始数据。

参考序列$a_0$即为优化目标，例如无量纲化处理后的1。

设评价对象有$m$个，评价指标有$n$个，分别为$x_i, i = 1, 2, \dots, n$

则比较序列$$a_{i} = \{a_i(j) | j = 1, 2, \dots, n\}$$其中，$a_i(j)$表示第$i$个评价对象关于第$j$个指标变量$x_j$的取值。

参考序列$$a_0(j) = \max_{1 \le i \le n}\{a_i(j)\}$$即为参考序列相当于一个虚拟评价最好对象的各指标值。

#### 第2步：确定各指标值的权重

这一步可选择项很多，例如层次分析法，熵权法等等。只要能顺利得到各指标值的权重即可。

#### 第3步：计算灰色关联系数

灰色关联系数计算：
$$\xi_i(j) = \frac{\min\limits_{1\le s \le m}\min\limits_{1\le t \le n}|a_0(t) - a_s(t)| + \rho\max\limits_{1\le s \le m}\max\limits_{1\le t \le n}|a_0(t) - a_s(t)|}{|a_0(j) - a_i(j)| + \rho\max\limits_{1\le s \le m}\max\limits_{1\le t \le n}|a_0(t) - a_s(t)|}, i = 1, 2, \dots, m, j = 1, 2, \dots, n.$$

$\xi_i(j)$表示比较序列$a_i$对于参考序列$a_0$在第$j$个指标上的关联系数，其中$\rho \in [0, 1]$为分辨系数。$\min\limits_{1\le s \le m}\min\limits_{1\le t \le n}|a_0(t) - a_s(t)|, \max\limits_{1\le s \le m}\max\limits_{1\le t \le n}|a_0(t) - a_s(t)|$分别被称为两极最小差和两极最大差。

分辨系数$\rho$的取值越大，分辨率越大。经验取值为$\rho = 0.5$.

#### 第4步：计算灰色加权关联度

灰色加权关联度的公式为$$r_i = \sum^{n}_{k = 1}w_k\xi_i(k)$$其中，$r_i$为第$i$个评价对象对参考对象的灰色加权关联度，$w_i$为第$i$个评价指标的权重。

#### 第5步：评价分析

根据$r_i$的值做出评价。$r_i$越大，关联度越高。

# 相关性分析方法

## 卡方独立性检验

### 适用情境

离散变量之间的相关性判断，需要统计频数作为数据支撑。

基本步骤略

此外，常规的卡方独立性检验只能判断二者是否具有相关性，但是不能描述二者的相关性强弱，因此通常还会引入关联强度指标。

### 关联强度指标

#### $\phi$系数

$$\phi = \sqrt{\frac{\chi^2}{N} }$$ 其中$N$表示样本总量。

$\phi$系数的取值范围一般在$0\sim 1$之间（2x2列联表中可能出现负值，但是一般取绝对值），值越大则关联程度越高。

#### Cramer's V系数

当列联表大于2x2时，我们需要使用Cramer's V系数来衡量关联强度。

$$V = \sqrt{ \frac{\chi^2 / N}{\min(r - 1, c - 1)} }$$
其中：
- $\chi^2$为计算得到的卡方值。
- $N$为样本总量。
- $r$为行数（变量一的类别）。
- $c$为列数（变量二的类别）。
- $\min(r - 1, c - 1)$为取（行数-1）和（列数-1）中较小的那个值。这实际上是卡方检验的自由度的上限，用于对公式进行标准化。

注意：无论是$\phi$系数还是Cramer's V系数，都只能反应关联强度的大小，不能反映正相关或负相关。

## 皮尔逊相关系数

### 使用场景

皮尔逊相关系数只能对连续型变量进行计算，主要用于衡量两个变量之间的**线性**关系。皮尔逊相关系数对噪点敏感，在使用时应当注意进行降噪处理，并且尽可能保证其数据分布基本符合正态分布。

### 基本步骤

皮尔逊相关系数$r$量化了两个变量协同变化的程度，计算方式如下：$$r = \frac{\sum^n_{i = 1}(x_i - \bar{x})(y_i - \bar{y})}{\sqrt{\sum^n_{i = 1}(x_i - \bar{x})^2}\sqrt{\sum^n_{i = 1}(y_i - \bar{y})^2}}$$

详细的说，可以按照下面的步骤运算：

1. 计算均值：计算$\bar{x}$与$\bar{y}$。

1. 计算离差：对于每个数据点，计算其与均值的差$x_i - \bar{x}$和$y_i - \bar{y}$。

1. 计算协方差项：将每对离差相乘，并求和，即$\sum (x_i - \bar{x})(y_i - \bar{y})$。

1. 计算标准差项：分别计算两个变量离差的平方和，再开方，即$\sqrt{\sum(x_i - \bar{x})^2}$和$\sqrt{\sum(y_i - \bar{y})^2}$）。

### 数据分析

协方差决定了相关性的方向（正或负）。

标准差的计算公式中，分母是两个标准差的乘积，用于标准化协方差，从而决定了相关性的强度。

对于皮尔逊相关系数，计算结果一定为$-1 \le r \le 1$,$r$的符号决定了两个变量是相关的方向，绝对值决定了两个变量相关性的强弱。一般而言，对于$|r|$的值对相关性的描述可以如下：

1. $|r| \ge 0.8| \sim$ 强相关

1. $0.5 \le |r| < 0.8 \sim$ 中等相关

1. $0.3 \le |r| < 0.5 \sim$ 弱相关

1. $|r| < 0.3$ 极弱或无相关

根据计算出的皮尔逊相关系数，也可以通过一个变量的值去预测另一个变量的可能取值。

### 图表呈现

对于皮尔逊相关系数的呈现，散点图是最好的呈现方式。

而且，最好在计算皮尔逊相关系数之前先作一次散点图，一方面为图形降噪作准备，另一方面可以大体观测是线性关系还是非线性关系（皮尔逊相关系数只能用于处理线性关系）。

## 斯皮尔曼相关系数

### 使用场景

斯皮尔曼相关系数的适用场景比皮尔逊更宽松，因此也更通用。

首先，斯皮尔曼相关系数可以处理两个联系变量、一个连续变量和一个有序分类变量、两个有序分类变量之间的相关关系。而且，它既不要求数据是否基本满足正态分布，也不要求相关关系是线性或非线性。它对异常值同样也不敏感，对噪点有一定抗性。

典型研究问题：

- 员工的工作年限（连续）与他们的**job满意度等级**（有序，1-5）是否相关？

- 投资者的风险偏好等级（有序，低/中/高）与其投资回报率（连续）之间是否存在关系？

- 探讨年龄（连续）与使用新科技的意愿等级（有序）之间的关联。

### 基本步骤

斯皮尔曼相关系数的核心思想是先将原始数据转化为等级数据，然后再计算这些等级之间的皮尔逊相关系数。

基本计算公式和皮尔逊相关系数是一致的：$$r = \frac{\sum^n_{i = 1}(x_i - \bar{x})(y_i - \bar{y})}{\sqrt{\sum^n_{i = 1}(x_i - \bar{x})^2}\sqrt{\sum^n_{i = 1}(y_i - \bar{y})^2}}$$

不过，由于使用了排名数据，因此可以进一步简化为：$$\rho = 1 - \frac{6\sum d_i^2}{n(n^2 - 1)}$$

皮尔逊相关系数$r$量化了两个变量协同变化的程度，计算方式如下：$$r = \frac{\sum^n_{i = 1}(x_i - \bar{x})(y_i - \bar{y})}{\sqrt{\sum^n_{i = 1}(x_i - \bar{x})^2}\sqrt{\sum^n_{i = 1}(y_i - \bar{y})^2}}$$

其中，
- $d_i$：表示一对观测值的等级差。
- $n$：表示总观测对数。

详细的说，可以按照下面的步骤运算：

1. 将数据转换为等级：
    将变量X和Y的数据分别从小到大排序并赋予排名（1为最小，n为最大）。
    注意：如果存在并列值，则取它们排名的平均值。

1. 计算等级差：对于每一对观测值，计算其X排名和Y排名的差$d_i$。

1. 计算等级差的平方和：将每个$d_i$平方后求和，即$\sum d_i^2$。

1. 代入公式计算$\rho$.

### 数据分析

对于斯皮尔曼相关系数的分析与皮尔逊相关系数基本一致。

其计算结果同样也一定为$-1 \le r \le 1$,$r$的符号决定了两个变量是相关的方向，绝对值决定了两个变量相关性的强弱。一般而言，对于$|r|$的值对相关性的描述可以如下：

1. $|r| \ge 0.8| \sim$ 强相关

1. $0.5 \le |r| < 0.8 \sim$ 中等相关

1. $0.3 \le |r| < 0.5 \sim$ 弱相关

1. $|r| < 0.3$ 极弱或无相关

### 图表呈现

对于斯皮尔曼相关系数的呈现，散点图仍然是最好的呈现方式。

此外，由于斯皮尔曼相关系数也可以研究非线性关系，因此在散点图中可以添加一条Loess曲线（原本在皮尔逊相关系数的散点图中应当为直线）来反应单调趋势。

# 往年题分析

## 2024-C

### 题目分析

题目为一个农作物种植的效益最大的决策问题。

#### 问题1：稳定情形下的种植优化

假定各种农作物未来的销售量，成本和价格与去年相对保持稳定，给出接下来2024~2030年农作物的最优种植方案。

- 目标：最大化收益，重点考虑销售量限制和销售方式两种情况

- 需回答：在如下两种情况下考虑如何让效益最大化
    1. 超出部分直接滞销，导致浪费；
    1. 超出部分可以半价出售

#### 问题2：考虑不确定性和波动下的最优种植方案

在成本、价格、产量、销量都存在不确定性的前提下，给出2024~2030年农作物的最优种植方案。

> 成本：平均每年增长$5\%$左右。
> 价格：粮食类基本稳定，蔬菜类每年增长$5\%$左右，食用菌每年下降$ 1\% \sim 5\% $，尤其羊肚菌每年下降$5\%$。
> 产量：每年大约有$\pm10\%$的波动。
> 销量：小麦玉米每年$5\% \sim 10\%$增长，其他农作物相比2023年大约有$\pm5\%$的波动。

- 目标：考虑风险，收益最大化。

#### 问题3：考虑作物之间的关系的最优种植方案

要求对农作物之间的关系进行建模分析，并且进行模拟数据进行求解，和问题2进行比较分析。

#### 附件：相关数据

附件给出了各种作物的相关数据，说明了作物种植的特殊条件。


### 问题1求解

#### 1. 数据清洗和处理

补全异常值。

#### 2. 混合整数规划（MIP）

构建种植优化问题的主模型，设计相关函数。

决策步骤：寻找决策变量、构建目标函数、限制约束条件。

#### 3. 模拟退火：进行全局优化求解（核心）

1. 设置初始最优解$\text{BestPlan}$，初始温度$\text{Temperture}$。

1. 主循环
    （1）依据当前温度对方案进行扰动，得到$\text{NewPlan}$。
    （2）适用目标函数计算$\text{NewPlan}$的收益$\text{NewProfit}$。
    （3）将两个方案的收益进行比较，并进行迭代：

    1. 若$\text{NewProfit} > \text{BestProfit}$，则将当前方案更新为$\text{BestPlan}$；
    1. 否则，根据两种方案的差值，进行概率更新。

    （4）降温，如$\text{Temperture = Temperture} \times 0.95$。

1. 返回最优解

### 问题2求解

本题额外添加了概率波动和风险评估，因此需要修改原有目标函数进行处理。

#### 蒙特卡罗模拟MCMC

利用蒙特卡洛模拟可以处理概率增长的数据，得到期望值和方差，用于构建目标函数。

#### NSGA-II求解（核心）

1. 定义问题类

    - 决策变量
    - 目标函数：最高收益，最小风险（方差）
    - 约束条件

1. 采样
    生成一系列可行方案作为初始种群。

1. 修复
    编写函数$\text{repairSolution}$，若解不符合条件，则调用修复。

1. 进化（主循环）
    （1）利用选择、交叉、变异生成新种群。
    （2）修复不满足约束的个体。
    （3）利用目标函数计算每个个体的收益（适应度$\text{Fitness}$），并淘汰部分个体。

1. 返回
    在多次迭代（如200代）后得出最优方案。


### 问题3求解

关键点在于如何构建农作物之间的关联变化。利用Spearman相关分析构建市场和自然波动场景，利用NSGA-II遗传算法求解最优方案。

大体步骤同上。

# 数据统计

1. 分布规律：

- 数据满足什么分布、不同数据之间是否属于同一分布
- 统计指标:最大值、最小值、均值、中位数、偏度系数、峰度系数、标准差
- 时间序列是否自相关
- 不同的时间尺度下的数据规律:
    + 销售量-天: 时间尺度最小，噪声最大，可以体现特殊节假日的影响
    + 销售量-周: 周一到周日，销售量一般集中在周末
    + 销售量-月: 季度规律
    + 销售量-年: 宏观环境、突发事件(疫情)


2. 相互关系:
- 皮尔相关性系数: 衡量线性关系的强度和方向。
- 斯皮尔曼等级相关系数: 适用于非线性但单调关系的情况。
- 肯尔t系数: 测量两个变量间排序一致性的强度。
- 回归分析: 建立变量间的线性或非线性关系模型。
- 主成分分析(PCA): 降维并揭示变量间的相关性结构。
- 因子分析: 识别隐藏的变量(因子)并分析它们对观测变量的影响。
- 聚类模型: 变量分类

最优化理论的三大启发式算法：
- 模拟退火（连续变量）
- 遗传算法（离散变量）
- 粒子群算法（多变量）


预测问题：
- 神经网络模型
- 灰色预测模型
- 拟合差值预测
- 时间序列模型
  + ARIMA(自回归积分滑动平均模型): 用于捕捉数据的趋势和季节性波动。
  + SARIMA(季节性ARIMA模型): 扩展ARIMA模型，适用于季节性数据
  + CETS(指数平滑状态空间模型): 用于捕捉趋势、季节性和周期性变化。
  + Prophet: 由Facebook开发的模型，适用于具有强季节性和假日效应的数据。（可靠）
  + LSTM(长短期记忆网络): 深度学习型，擅长处理长时间依赖关系的列数据
- 马尔科夫模型
- 支持向量机模型
- Logistic模型
- 组合预测模型
- 微分方程预测

混合整数二次规划

数据少的砍掉，当成缺数据处理。


层次聚类 & 热力图